# Example environment variables for local development

# Backend / FastAPI
SECRET_KEY=changeme
OPENAI_API_KEY=
KMS_PROVIDER=local

# Postgres (local docker-compose)
POSTGRES_PASSWORD=postgres
DATABASE_URL=postgresql://cyberia:postgres@localhost:5432/cyberia

# MinIO (local)
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin

# AWS (for terraform/app when deployed)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
KMS_KEY_ID=

# Railway
RAILWAY_TOKEN=
